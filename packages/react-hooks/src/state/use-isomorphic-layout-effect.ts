import { useIsoLayoutEffect } from "../internal/mod.ts";

/**
 * React layout effect in the browser and regular effect during server rendering.
 */
export const useIsomorphicLayoutEffect = useIsoLayoutEffect;

{
  "name": "temelj",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "turbo run build",
    "clean": "turbo run clean --parallel",
    "dev": "turbo dev --continue",
    "dev:packages": "turbo dev --continue --filter './packages/*'",
    "typecheck": "turbo run typecheck --parallel",
    "format": "biome format --write .",
    "format:check": "biome format .",
    "lint": "biome lint",
    "lint:fix": "biome lint --write --unsafe .",
    "prestart": "cp scripts/git/pre-commit .git/hooks/ && chmod +x .git/hooks/pre-commit && echo 'hook copied'",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.4",
    "@types/node": "24.10.0",
    "shx": "^0.3.4",
    "tsx": "^4.20.6",
    "turbo": "^2.6.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.8"
  },
  "packageManager": "pnpm@10.21.0"
}
